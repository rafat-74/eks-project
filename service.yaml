apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: my-web-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer # ده هيخلي AWS تعمل لنا IP حقيقي